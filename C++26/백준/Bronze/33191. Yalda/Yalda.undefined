#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr), cout.tie(nullptr);
    int a;
    cin >> a;
    int t;
    string s[] = {"Watermelon", "Pomegranates", "Nuts", "Nothing"};
    for (int i = 0; i < 3; ++i)
    {
        cin >> t;
        if (t <= a)
        {
            cout << s[i];
            return 0;
        }
    }
    cout << s[3];
    return 0;
}